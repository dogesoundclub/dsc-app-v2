import { View, ViewParams } from "skyrouter";
export default class MateHolders implements View {
    private container;
    private list;
    constructor(params: ViewParams);
    private load;
    changeParams(params: ViewParams, uri: string): void;
    close(): void;
}
//# sourceMappingURL=MateHolders.d.ts.map